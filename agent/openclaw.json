{
  "name": "neurvinial",
  "version": "1.0.0",
  "description": "Autonomous AI Lending Agent for the Agent Economy",
  "skills": {
    "entries": {
      "neurvinial_credit": {
        "enabled": true,
        "description": "Credit assessment skill - evaluates agent creditworthiness",
        "env": {
          "Neurvinial_API_URL": "${Neurvinial_API_URL:-http://localhost:3000}"
        }
      },
      "neurvinial_lending": {
        "enabled": true,
        "description": "Lending decision skill - processes loan requests autonomously",
        "env": {
          "Neurvinial_API_URL": "${Neurvinial_API_URL:-http://localhost:3000}"
        }
      },
      "neurvinial_recovery": {
        "enabled": true,
        "description": "Recovery and liquidation skill - handles overdue loans",
        "env": {
          "Neurvinial_API_URL": "${Neurvinial_API_URL:-http://localhost:3000}",
          "TELEGRAM_BOT_TOKEN": "${TELEGRAM_BOT_TOKEN}"
        }
      },
      "neurvinial_wdk": {
        "enabled": true,
        "description": "WDK wallet operations - on-chain USDT transfers and balances",
        "env": {
          "Neurvinial_API_URL": "${Neurvinial_API_URL:-http://localhost:3000}",
          "WDK_NETWORK": "${WDK_NETWORK:-sepolia}"
        }
      },
      "neurvinial_bot_commands": {
        "enabled": true,
        "description": "Intelligent bot command handling with context-aware reasoning",
        "env": {
          "Neurvinial_API_URL": "${Neurvinial_API_URL:-http://localhost:3000}"
        }
      }
    }
  },
  "tools": {
    "wdk": {
      "enabled": true,
      "description": "Tether WDK wallet operations",
      "source": "@tetherto/wdk-agent-skills"
    }
  }
}
